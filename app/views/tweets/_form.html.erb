<%= form_for @tweet do |f| %>
    <%= text_field_tag 'tweet[genre_list]', "", placeholder: 'genre' %>

    <%= f.text_area :text, placeholder: "コメント" %>

    <%= f.file_field :upload_file %>

  <%= f.submit "投稿する"%>
<% end %>